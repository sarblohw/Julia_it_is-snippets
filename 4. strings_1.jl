str = "sammy\nsam"
str2 = "sss"

println(length(str))
println(str)
println(str2)
println(length(str2))
print(str)
print(str2)
print("sam")


#println - prints a new line at the end of the output

#=
printf() is primarily needed when 
you need to print big strings to 
avoid string concatenaion in 
println() which can be confusing at times. 
(Although both can be used in almost all cases)
=#